version: '2'
services:
  reactdemo-mongodb:
    image: mongo:4.0.16
    ports:
      - '27017:27017'
    command: mongos --configdb reactdemo-mongodb-config
  reactdemo-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --replSet rs1 --noprealloc --smallfiles
  reactdemo-mongodb-config:
    image: mongo:4.0.16
    container_name: reactdemo-mongodb-config
    command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
